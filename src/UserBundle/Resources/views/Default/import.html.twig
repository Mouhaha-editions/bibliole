{% extends 'base.html.twig' %}

{% block content %}
    <div class="row">
        <div class="col-md-12">

            <div class="card clearfix">
                <div class="header">
                    <h2>
                        Import O.N.D.E.
                    </h2>
                </div>
                <div class="body">
                    <div class="col-md-6">
                        {{ form_start(form) }}
                        {{ form_widget(form) }}
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" class="btn btn-success"
                                                                      value="Importer"/>&nbsp;
                        </div>
                        {{ form_end(form) }}                    </div>
                    <div class="col-md-6">
                        <div class="alert alert-info">
                            <h2>Nos garanties</h2>
                            <p>Nous savons que les données que vous importez sont sensibles, c'est pourquoi nous n'importons QUE le nom, le prénom et la classe de l'élève.<br/>
                            Le fichier n'est stocké que le temps de faire l'import, il est supprimé ensuite.</p>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
